# Package Requirements for Development
# Include all the libraries we're using in production.
-r production.txt

bpython == 0.12
django-debug-toolbar == 0.9.4
django-debugtools == 1.1.0
django-devserver == 0.6.2
django-extensions == 1.1.1
